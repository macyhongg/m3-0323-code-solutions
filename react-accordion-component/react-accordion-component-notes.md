# react-accordion-component-notes

## Notes

All student notes should be written here.


How to write `Code Examples` in markdown

for JS:
```javascript
const data = "Howdy"
```

for HTML:
```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:
```css
div {
  width:100%
}
```

<!-- - prop = topics as an array of objects -->
component accordion
render topics in separate component
app.js define list of topics (array of objects; {topic, details} )
